build:
  opts: '[dir]'
  desc: build project from current or specified directory
cache:
  opts: '<key> [value]'
  desc: 'read/write value from/to cache'
clean:
  opts: '[dir]'
  desc: clean project from current or specified directory
help:
  desc: get help
info:
  opts: '<collection|module>'
  desc: gets info for a collection or module
init:
  opts: '[dir]'
  desc: initialize a crystal project
install:
  opts: '[generator]'
  desc: install a generator
link:
  opts: '<module>@<version> [src]'
publish:
  opts: '[dir]'
  desc: publish project from current or specified directory
run:
  opts: '[dir]'
  desc: run project from current or specified directory
search:
  opts: '[generator]'
  desc: search for public generators
signup:
  desc: signup for crystal
spec:
  desc: get code specification by name
test:
  desc: test project
update:
  desc: update generators
version:
  opts: '<version>'
  desc: update project's version