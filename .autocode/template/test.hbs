import Autocode from '../../../src/autocode'
import Chai from 'chai'

describe('{{{description}}}', _ => {
  {{#each tests}}
  it{{#if only}}.only{{/if}}('{{{it}}}', done => {
    let autocode = new Autocode({{#json}}{{{data}}}{{/json}})
    {{#if expects}}
    {{#each expects}}
    expects(autocode).to.equal({{#json}}{{{data}}}{{/json}})
    {{/each}}
    {{/if}}
    done()
  })
  {{/each}}
})
