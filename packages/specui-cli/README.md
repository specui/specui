# @spec-ui/cli

> the CLI of SpecUI

## Quickstart

### Use `npx`

```
npx @specui/cli [command]
```

### Install Globally

First install:

```
npm i -g specui
```

Then run:

```
specui [command]
```

## Commands

### `generate`

Generates UI from spec

### `new`

Creates a new SpecUI project
